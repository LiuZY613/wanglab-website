image: node:20

pages:
  stage: deploy
  script:
    - npm install
    - npm run build
    - cp -r docs/.vitepress/dist public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
